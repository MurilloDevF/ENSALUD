export type CalculationSummary = { processed: number; durationMs: number };

export async function runBudgetCalculations(): Promise<CalculationSummary> {
  // TODO: Cálculos presupuestales y agregaciones (RF4)
  return { processed: 0, durationMs: 0 };
}
